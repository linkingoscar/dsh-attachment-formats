## What
<!-- One line: format/provider/UX + why searchable: `dsh pdf` `dsh ocr` -->

## Why (retrieval)
<!-- Which search query should find this PR? -->

## Changes
- [ ] `pnpm run lint` / `typecheck` / `build:client` / `verify:build`
- [ ] `pnpm run smoke:all` (host/route/client/ocr/p0)
- [ ] README keywords updated if user-facing (`dsh-plugin`, `deepseek-harness`)

## Test
<!-- File type, size, provider, dsh version -->

## Checklist
- [ ] No secrets in diff
- [ ] `lib/client.js` rebuilt if `src/client` changed
